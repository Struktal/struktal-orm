{
    "name": "struktal/struktal-database-objects",
    "description": "PHP library implementing the DAO pattern for easier database handling",
    "keywords": [
        "database",
        "objects"
    ],
    "type": "library",
    "license": "MIT",
    "autoload":{
        "psr-4": {
            "struktal\\DatabaseObjects\\": "src/",
            "struktal\\DatabaseObjects\\Database\\": "src/database/"
        }
    },
    "require": {
        "php": ">=8.2.0"
    },
    "require-dev": {
        "pestphp/pest": "~v3.8.2"
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "scripts": {
        "test": [
            "vendor/bin/pest"
        ]
    }
}
